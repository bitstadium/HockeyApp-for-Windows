<UserControl x:Class="HockeyApp.AppLoader.Views.ConfigurationContentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:cal="http://www.caliburnproject.org"
             
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <TabControl>
        <TabItem Header="Apps"  cal:View.Model="{Binding AppsView}"/>
        <TabItem Header="Feedback" cal:View.Model="{Binding FeedbackView}"/>
        <TabItem Header="Settings" cal:View.Model="{Binding SettingsView}"/>
    </TabControl>
</UserControl>
