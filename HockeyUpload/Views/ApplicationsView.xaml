<UserControl x:Class="HockeyApp.AppLoader.Views.ApplicationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:util="clr-namespace:HockeyApp.AppLoader.Util"
             mc:Ignorable="d" 
             Width="1280" Height="800"
             d:DesignHeight="800" d:DesignWidth="1280">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources.xaml"/>
                <ResourceDictionary Source="MyIcons.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid Margin="20,0,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="450"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="300*"/>
            <ColumnDefinition Width="20"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="245*"/>
        </Grid.RowDefinitions>
        

        <TextBlock Text="Configurations" FontFamily="Segoe UI" FontSize="24" Grid.Row="0"/>
        <ListBox FontFamily="Segoe UI" Grid.Row="1" ItemsSource="{Binding UserConfigurations}" SelectedItem="{Binding SelectedUserConfiguration}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="50"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="30"/>
                            <RowDefinition Height="20"/>
                        </Grid.RowDefinitions>
                        <ContentPresenter Content="{StaticResource DefaultUserIcon}" Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" Margin="3"/>
                        <TextBlock Text="{Binding ConfigurationName}" FontSize="20" Grid.Column="1" Grid.Row="0"/>
                        <TextBlock Text="{Binding UserName}" FontSize="16" Grid.Column="1" Grid.Row="1"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <MenuItem cal:Message.Attach="RefreshApps" Grid.Column="2" Grid.Row="0"  VerticalAlignment="Top" HorizontalAlignment="Right">
            <MenuItem.Header>
                <StackPanel Orientation="Horizontal">
                    <ContentPresenter Content="{StaticResource ResourceKey=RefreshMenuIcon}" Width="15"/>
                    <TextBlock Text="refresh" Margin="5,0,0,0"/>
                </StackPanel>
            </MenuItem.Header>
        </MenuItem>
        
        <TextBox Controls:TextboxHelper.Watermark="Quickfilter" 
                 Grid.Column="2" Grid.Row="0"
                 Text="{Binding FilterString, UpdateSourceTrigger=PropertyChanged}" 
                 VerticalAlignment="Bottom" Margin="0,0,0,5"/>

        <ListBox Grid.Row="1" Grid.Column="2" ItemsSource="{Binding Apps}" IsSynchronizedWithCurrentItem="True">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="50"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid Grid.Column="0" Margin="3">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="4"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="20"/>
                                <RowDefinition Height="20"/>
                                <RowDefinition Height="4"/>
                            </Grid.RowDefinitions>
                            <Image Source="{Binding AppImage}" Grid.ColumnSpan="2" Grid.RowSpan="2"/>
                            <Image Source="{Binding PlatformIcon}" Grid.Column="1" Grid.Row="1" Grid.ColumnSpan="2" Grid.RowSpan="2"/>
                        </Grid>
                        <TextBlock FontSize="16" Grid.Column="1" Text="{Binding Title}"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <ContentControl Margin="0,10,0,0" cal:View.Model="{Binding Apps.CurrentItem}" Grid.Column="4" Grid.Row="0" Grid.RowSpan="2"/>
    </Grid>


<!--


    <Grid Margin="10,20,0,10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="35"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Quickfilter" VerticalAlignment="Center" Grid.Column="0" Grid.Row="0" HorizontalAlignment="Left"/>
            <TextBox Text="{Binding FilterString, UpdateSourceTrigger=PropertyChanged}"
                     HorizontalAlignment="Stretch"
                     Margin="65,3,2,4"
                     VerticalAlignment="Stretch" Grid.Column="0" Grid.Row="0"/>
            <util:SortableListView IsSynchronizedWithCurrentItem="True" ItemsSource="{Binding Apps}" SelectedItem="{Binding SelectedApp}" Grid.Row="1" Margin="2,0,2,2" HorizontalAlignment="Stretch">
                <util:SortableListView.View>
                    <GridView>
                        <GridViewColumn Header="Title" DisplayMemberBinding="{Binding Title}" Width="150"/>
                        <GridViewColumn Header="Platform" DisplayMemberBinding="{Binding Platform}" Width="150"/>
                    </GridView>
                </util:SortableListView.View>
            </util:SortableListView>
            <Grid Grid.Row="2">
                <ComboBox ItemsSource="{Binding UserConfigurations}" 
                      Margin="2,0,2,0"
                      DisplayMemberPath="ConfigurationName" 
                      SelectedItem="{Binding SelectedUserConfiguration}"
                      HorizontalAlignment="Stretch"
                      cal:Message.Attach="[Event SelectionChanged] = [SelectedUserConfiguration_OnSelectionChanged]"/>
            </Grid>
        </Grid>
        <GridSplitter Width="1" Grid.Column="0" HorizontalAlignment="Right"/>
        <ContentControl cal:View.Model="{Binding Apps.CurrentItem}" Grid.Column="1" Margin="20,0,0,0"/>
    </Grid>
-->
</UserControl>
