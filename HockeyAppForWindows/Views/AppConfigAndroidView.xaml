<UserControl x:Class="HockeyApp.AppLoader.Views.AppConfigAndroidView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="110"/>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="Update" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Height="16" Margin="10,0,0,0"/>
        <CheckBox Content="Mandatory" IsChecked="{Binding IsMandatory}" Grid.Row="0" Grid.Column="1" VerticalAlignment="Center"/>
    </Grid>
</UserControl>
